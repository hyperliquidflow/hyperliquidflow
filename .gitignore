# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/

# env files — NEVER commit these
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# logs
npm-debug.log*
.env*.local

# git worktrees
.worktrees/

# superpowers brainstorm artifacts
.superpowers/

# local dev databases
local.db

# claude settings (local only)
.claude/settings.json

# CI artifacts regenerated per run
learning-summary.json
scan-summary.json
skill-test-summary.json
wallet-signal-stats-summary.json

# editor workspace files (user-local)
*.code-workspace

# claude worktrees
.claude/worktrees/

# one-off remediation audit, permanent copy lives in docs/audit/
restore-liq-false-positives.json

# scratch probes (never commit)
.probe.ts

# fill study local cache (large, regenerable)
fill-study-cache.json

# funding history cache (regenerable)
funding-cache.json
